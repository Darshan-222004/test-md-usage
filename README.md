A Markdown (.md) file is a plain text file that uses the Markdown syntax to format text.
 This format is widely used for documentation, README files in repositories, and wikis.
 The syntax allows for simple formatting like headings, lists, links, and code blocks, making it easy to write and read while still being converted into richly formatted HTML.
